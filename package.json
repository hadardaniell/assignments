{
  "name": "assignments-root",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node index.js",
    "build": "tsc",
    "test": "npm --prefix server test",
    "test:watch": "npm --prefix server run test:watch"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}